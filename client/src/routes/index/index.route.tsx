import { FC, ReactElement } from 'react';

const IndexRoute: FC = (): ReactElement => {
    return (
        <div>
            <p>Index route</p>
        </div>
    );
};

export default IndexRoute;