import { FC, ReactElement } from 'react';

const SignupRoute: FC = (): ReactElement => {
    return (
        <div>
            <p>SignUp route</p>
        </div>
    );
};

export default SignupRoute;
