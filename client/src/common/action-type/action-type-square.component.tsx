import { FC, ReactElement } from 'react';

type ActionTypeSquareProps = {}

const ActionTypeSquare: FC<ActionTypeSquareProps> = (): ReactElement => {
    return (
        <div>

        </div>
    );
};

export default ActionTypeSquare;