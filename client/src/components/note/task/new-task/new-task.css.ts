import { rem } from '@mantine/core';
import { style } from '@vanilla-extract/css';

export const NewTaskIconCss = style({
    width: rem(16),
    height: rem(16),
    position: 'relative',
    top: rem(8)
})